def test_response_types(raw, extended_trials_df):
    raw_params = raw["items"]["behavior"]["params"]
    raise Exception(raw["response_time"])
    for trial in raw["items"]["behavior"]["trial_log"]:
        events = trial["events"]
